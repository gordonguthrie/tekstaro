<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Tekstaro - a corpus of Esperanto usage</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <%= csrf_meta_tag() %>
  </head>
  <body>
    <header>
      <section class="container">
        <nav role="navigation">
          <ul>
            <li><a href="https://github.com/gordonguthrie/radikigo"><%= gettext "On Github" %></a></li>
            <li><a href="/upload"><%= gettext "Upload Text" %></a></li>
            <!-- <%= sign_in  = gettext("Sign in")  %> -->
            <!-- <%= sign_out = gettext("Sign out") %> -->
            <!-- <%= sign_up  = gettext("Sign up") %> -->
            <%= if signed_in?(@conn) do %>
              <li><%= link sign_in,  to: Routes.session_path(@conn, :new, @locale) %></li>
            <% else %>
              <li><%= link sign_out, to: Routes.session_path(@conn, :delete, @locale), method: :delete %></li>
              <li><%= link sign_up,  to: Routes.user_path(@conn, :new, @locale) %></li>
            <% end %>
          </ul>
        </nav>
        <a href="/" class="phx-logo">
          <img src="<%= Routes.static_path(@conn, "/images/tekstaro.png") %>" alt="Tekstaro Logo"/>
        </a>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
